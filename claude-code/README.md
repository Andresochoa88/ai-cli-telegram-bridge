# claude-code/

Claude Code implementation of the Telegram bridge.

See the [root README](../README.md) for full setup instructions, architecture explanation, and troubleshooting.

## Quick start

```bash
bash install.sh
```

The installer will ask for your bot token and the `CLAUDE_CWD` — the directory Claude Code uses as its working directory (where your `CLAUDE.md` and memory live).
