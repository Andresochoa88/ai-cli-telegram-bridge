# codex/

Codex CLI implementation of the Telegram bridge.

See the [root README](../README.md) for full setup instructions, architecture explanation, and troubleshooting.

## Quick start

```bash
bash install.sh
```

The installer will ask for your bot token and the `CODEX_CWD` — the directory Codex uses as its context root (where your agent's memory and configuration live).
